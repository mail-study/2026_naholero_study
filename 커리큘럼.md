0단계는 "이론 이해"보다 **손이 기억하게 만드는 단계**입니다. 목차를 두 축으로 나눠서 정리했습니다.

## A. PyTorch 기본기 (약 5~7일)

**1. 텐서 다루기**

- 생성 방법(`zeros`, `ones`, `randn`, `arange`), dtype과 device 개념
- shape 조작: `view` vs `reshape`, `squeeze`/`unsqueeze`, `permute`/`transpose`
- 인덱싱, 슬라이싱, 마스킹
- 브로드캐스팅 규칙 (여기서 막히는 버그가 제일 많습니다)
- 행렬곱: `matmul`, `@`, `bmm`, 여유되면 `einsum` 맛보기
- numpy ↔ tensor 변환과 메모리 공유 이슈

**2. Autograd**

- `requires_grad`, `.backward()`, `.grad`의 관계
- 동적 계산 그래프가 언제 만들어지고 언제 해제되는지
- `detach()`, `torch.no_grad()`를 언제 쓰는지
- gradient가 누적된다는 사실과 `optimizer.zero_grad()`의 위치

**3. nn.Module과 학습 구성요소**

- `nn.Linear`, 활성화 함수, `nn.Sequential`
- 커스텀 모듈 만들기: `__init__` / `forward`
- `parameters()`, `state_dict()`, 저장과 로드
- 손실 함수: `MSELoss`, `CrossEntropyLoss` (**logit을 넣어야지 softmax 통과한 값을 넣으면 안 됨**)
- 옵티마이저: `SGD`, `Adam`과 `zero_grad → backward → step` 순서

**4. 데이터 파이프라인**

- `Dataset` 상속해서 `__len__`, `__getitem__` 구현
- `DataLoader`의 `batch_size`, `shuffle`, `num_workers`, `drop_last`
- `torchvision.transforms` 기본 사용법

**5. 학습 루프 템플릿 완성**

- `model.train()` / `model.eval()` 차이
- GPU 이동(`.to(device)`)과 텐서/모델 device 불일치 에러
- validation 루프, 체크포인트 저장
- seed 고정으로 재현성 확보

## B. 확률·통계 최소치 (약 3~4일)

**1. 확률 기본**

- 확률변수, 결합/조건부/주변확률, 독립
- 베이즈 정리 (생성 모델이나 분류기 해석할 때 계속 나옵니다)

**2. 기댓값과 분산**

- 기댓값, 분산, 공분산의 정의와 선형성

**3. 주요 분포**

- 베르누이, 카테고리컬, 가우시안 세 개면 당분간 충분

**4. MLE와 손실 함수의 연결** ← 이 파트가 핵심입니다

- 가우시안 가정 + MLE → MSE 손실이 유도되는 과정
- 카테고리컬 가정 + MLE → 크로스엔트로피가 유도되는 과정
- "손실 함수는 임의로 정한 게 아니라 확률 모델에서 나온다"는 감각 잡기

**5. 정보이론 조금**

- 엔트로피, 크로스엔트로피, KL divergence의 관계
- softmax와 log-softmax, log-sum-exp 수치 안정성

## 마무리 체크포인트 (통과 기준)

아래 네 개를 **검색 없이** 처음부터 칠 수 있으면 1단계로 넘어가도 됩니다.

1. numpy만으로 선형회귀 경사하강법 구현
2. 같은 걸 PyTorch autograd로 다시 구현하고 결과 일치 확인
3. MNIST MLP 학습 루프를 튜토리얼 복붙 없이 작성
4. 크로스엔트로피를 직접 구현해서 `nn.CrossEntropyLoss` 출력과 소수점 아래까지 맞춰보기

4번을 하다 보면 logit/확률/로그확률이 어디서 어떻게 변환되는지가 정리돼서, 나중에 detection이나 action recognition 코드 읽을 때 손실 함수 부분에서 안 막힙니다.
